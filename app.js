window.PFHApp=(function(){
 const $=s=>document.querySelector(s);
 function clock(){let d=new Date();$("#dateText").textContent=d.toLocaleDateString("en-US",{weekday:"long",month:"long",day:"numeric",year:"numeric"});$("#timeText").textContent=d.toLocaleTimeString("en-US",{hour:"numeric",minute:"2-digit"})}
 function authUI(){$("#connectionBtn").textContent=PFHGoogle.isConnected()?"● Connected ↻":"Reconnect Google";$("#connectionBtn").style.color=PFHGoogle.isConnected()?"#239254":"#a35a2b"}
 async function syncAll(){authUI();if(!PFHGoogle.isConnected()){PFHCalendar.render();PFHChores.render();PFHRecipes.render();PFHMeals.render();PFHLists.render();return}try{let items=await PFHGoogle.list();const moved=await PFHFirebase.migrateLegacy(items);if(moved)items=await PFHGoogle.list();PFHCalendar.loadFromItems(items);PFHChores.loadFromItems(items);await Promise.all([PFHRecipes.sync(),PFHMeals.sync(),PFHLists.sync()]);authUI()}catch(e){authUI();console.error(e);alert("Family Hub sync issue: "+e.message)}}
 function pages(){document.querySelectorAll(".bottomNav button").forEach(b=>b.onclick=()=>{document.querySelectorAll(".bottomNav button").forEach(x=>x.classList.toggle("active",x===b));document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));$("#"+b.dataset.page+"Page").classList.add("active")})}
 function boot(){clock();setInterval(clock,30000);PFHCalendar.init();PFHChores.init();PFHRecipes.init();PFHMeals.init();PFHLists.init();PFHWeather.init();pages();PFHGoogle.init();try{PFHFirebase.init()}catch(e){console.error(e)}authUI();$("#connectionBtn").onclick=()=>PFHGoogle.isConnected()?syncAll():PFHGoogle.connect();window.addEventListener("pfh-auth",()=>{authUI();syncAll()});setTimeout(()=>{PFHGoogle.init();authUI();if(PFHGoogle.isConnected())syncAll()},700)}
 document.addEventListener("DOMContentLoaded",boot);return {syncAll};})();
